import { PageParams } from "../constants/types";
import { NextPage } from "next";

const PortfolioPage: NextPage<PageParams> = ({ params }) => {
  return <div>PortfolioPage</div>;
};

export default PortfolioPage;
